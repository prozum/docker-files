# Dockerfiles

This repository contains the Dockerfiles for all the services, running on the servers, controlled by Kubernetes.

We are trying to ensure that all containers will share a single SQL database
